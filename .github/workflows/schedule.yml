name: Check OIIX NOTAMs

on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run bot script
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          GEMINI_API_KEY_F92: ${{ secrets.GEMINI_API_KEY_F92 }}
          GEMINI_API_KEY_F1: ${{ secrets.GEMINI_API_KEY_F1 }}
          GEMINI_API_KEY_FRED: ${{ secrets.GEMINI_API_KEY_FRED }}
          GEMINI_API_KEY_APHR: ${{ secrets.GEMINI_API_KEY_APHR }}
          GEMINI_API_KEY_MARZI: ${{ secrets.GEMINI_API_KEY_MARZI }}
          GEMINI_API_KEY_REZA: ${{ secrets.GEMINI_API_KEY_REZA }}
          GEMINI_API_KEY_MARY: ${{ secrets.GEMINI_API_KEY_MARY }}
        run: python main.py

      - name: Save state
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          
          git add *.json || true
          git add index.html || true
          
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update state, AI logs, and maps" && git push)
